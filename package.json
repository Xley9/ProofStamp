{
  "name": "proofstamp",
  "version": "1.0.0",
  "description": "ProofStamp is a privacy-first Progressive Web App (PWA) that lets you capture, hash, and securely store digital evidence — entirely offline and on your device.",
  "main": "sw.js",
  "scripts": {
    "build": "node scripts/build.js",
    "cap:sync": "npm run build && npx cap sync android",
    "cap:open": "npx cap open android",
    "cap:run": "npx cap run android"
  },
  "keywords": ["evidence", "proof", "hash", "sha256", "pwa", "camera", "gps"],
  "author": "Xley9",
  "license": "MIT",
  "dependencies": {
    "@capacitor/android": "^8.0.2",
    "@capacitor/camera": "^8.0.0",
    "@capacitor/cli": "^7.4.5",
    "@capacitor/core": "^8.0.2",
    "@capacitor/filesystem": "^8.1.0",
    "@capacitor/geolocation": "^8.0.0",
    "@capacitor/share": "^8.0.0"
  }
}
